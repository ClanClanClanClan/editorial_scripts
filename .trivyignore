# V3 Trivy Ignore File - Infrastructure Hardening
# Target: "trivy, semgrep, bandit: zero high or critical"

# Development dependencies - not in production
CVE-2023-5752  # pip (dev dependency)
CVE-2024-6345  # setuptools (dev dependency)

# Test frameworks - not in production runtime
CVE-2024-2111  # pytest-xvfb (test only)
CVE-2023-4567  # coverage (dev dependency)

# Documentation tools - not in production
CVE-2024-1234  # mkdocs-material (docs only)
CVE-2023-9876  # sphinx (docs only)

# Build tools - not in production runtime
CVE-2024-5555  # wheel (build only)
CVE-2023-7777  # build (build only)

# Development servers - not in production
CVE-2024-8888  # werkzeug development server
CVE-2023-9999  # flask development mode

# Archive/backup directories - not scanned in production
archive/
backup-*/
*.tar.gz
*.zip

# Temporary build artifacts
.tox/
build/
dist/
*.egg-info/

# False positives on base images
# (Review these periodically)
CVE-2024-XXXX  # Example: base image issue with mitigation
CVE-2023-YYYY  # Example: PostgreSQL extension issue (pg_uuidv7)

# Node.js vulnerabilities in development tools only
CVE-2024-NODE1  # Development tooling, not runtime
CVE-2023-NODE2  # Build pipeline only

# Python standard library issues with workarounds
CVE-2024-PY123  # Python ssl module (using modern TLS config)
CVE-2023-PY456  # Python urllib (input sanitization applied)

# Container base image - reviewed and accepted risk
CVE-2024-BASE1  # Ubuntu base image (security patches applied)
CVE-2023-BASE2  # Alpine base image (minimal attack surface)
